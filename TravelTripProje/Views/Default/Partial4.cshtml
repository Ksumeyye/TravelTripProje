@using TravelTripProje.Models.Siniflar
@model List<Blog>
<div class="padding1">
    <h4>Benim İçin İlk 6</h4> 
    <div class="box-left">
        @foreach (var x in Model)
        {
            <div class="bot-img">
                <img src="@x.BlogImage" alt="" style="border-radius:15px; width:120px; height:75px; object-fit:cover;"/>
            </div>
            <div class="bot-desc">
                <p>@x.Aciklama.Substring(0,75)</p> <h1><a href="/Blog/BlogDetay/@x.ID">>>></a></h1>
            </div>
            <div class="clear"></div>
        }
        </div>
            @Html.Action("Partial5","Default")
</div>
